<%= form_for(@bunch_type) do |f| %>
  <% if @bunch_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bunch_type.errors.count, "error") %> prohibited this form type from being saved:</h2>

      <ul>
      <% @bunch_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.fields_for :fields do |builder| %>
    <%= render 'field_fields', f: builder %>
  <% end %>
  <%= link_to_add_fields "Add Field", f, :fields%>

  <div class="actions">
    <%= f.submit "Save form",:class => 'btn btn-success' %>
  </div>
<% end %>
